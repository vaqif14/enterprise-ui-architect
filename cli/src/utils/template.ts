/**
 * Minimal template utilities.
 * Currently a passthrough; reserved for future Mustache/Handlebars-style rendering.
 */

export function renderTemplate(template: string, _variables: Record<string, string>): string {
  // Placeholder for future variable substitution
  return template;
}
